#include <iostream>

#include "Animal.h"

using namespace std;

int main() {
  // object cannot be created for animal class as it is an abstract class (has
  // pure virtual function)

  // testing for Animal class is in the child class (YoungAnimal & GrownAnimal)

  return 0;
}